{
  "name": "mdzip-editor-workspace",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "build": "npm run build -w mdzip-editor && npm run build -w @mdzip/editor-ng && npm run build -w @mdzip/editor-react && npm run build -w @mdzip/editor-vue && npm run build -w mdzip-editor-demo",
    "test": "npm run test -w mdzip-editor && npm run check:boundaries",
    "verify": "npm run build && npm run test",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "start:demo": "npm run build -w mdzip-editor && npm run build -w @mdzip/editor-ng && vite serve demo --config demo/vite.config.ts --port 4200"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^20.3.27",
    "@angular/cli": "^20.3.27",
    "@angular/compiler-cli": "^20.3.24",
    "ng-packagr": "^20.3.0",
    "playwright": "^1.60.0",
    "typescript": "^5.9.3"
  }
}
